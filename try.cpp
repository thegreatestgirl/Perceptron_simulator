#include <iostream>
#include <string>
#include <vector>

int main() {
    int rows, columns;
    std::cin >> rows >> columns;
    std::string str;
    std::cin >> str;
    std::vector<std::vector<char>> spiral;
    
    return 0;
}